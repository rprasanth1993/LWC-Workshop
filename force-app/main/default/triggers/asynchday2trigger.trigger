trigger asynchday2trigger on Account (before insert,after insert,before update,after update,before delete,after delete,after undelete){
    
   /* switch on Trigger.operationType{
        when after_update{
            //future method call
            //string jsonString = JSON.serialize(Trigger.new);//strong as a variable in jsonString..string because create account is string
            //asynchday2future.futuremethod(jsonString);
        }
    }*/

}